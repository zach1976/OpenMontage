# OpenMontage

## 💰 会产生费用的操作：必须三次确认 ★强制（2026-08-26 用户指令）

**任何会产生真实费用的操作（计费 API 调用、enable 计费服务、云资源创建、付费下单等），
必须获得用户三次独立的明确许可才可以执行。** 细则见
[`zachs_app_base.md`](见本项目引用路径) 同名章节;免费替代映射见
`AlphabetLearningFramework/docs/cost_audit_2026-08-26.md`。

## 📺 每个系列至少 5 条，依次做 ★强制（2026-08-26 用户指令）

系列立项时就排好**至少 5 期**选题，排完**直接依次生产，不要逐期停下来问顺序**。
顺序由内容逻辑决定（前一期的钩子引出下一期）。内容可以给用户过目，
但**不要做成阻塞的门**——做完连片子一起交，要改再改（管线零成本）。

## 🎨 同一系列每集必须换视觉语言 ★强制（2026-08-26 用户指令）

**不能拿上一集的版式换个词表就发。** 每集必须变：版式结构、配色、动效语言、信息编排；
变化要从**该集内容自己的世界**里长出来，不是随机换皮。

每集必须**不变**的（系列身份）：顶部品牌行、片尾 app icon + CTA、封面即第一帧、
词读两遍 + 跟读空档的教学节奏、y=240–1380 安全区、不提评论区。

**收尾自检**：这一集的画面能不能直接安到上一集头上？能——就是没做到位，回去重做版式。
（与 `AGENT_GUIDE.md` 的 atelier 原则一致：复用引擎知识，绝不复用创意组件。）

## ✂️ 画面文字不是旁白字幕 ★强制（2026-08-27 用户指令）

**画面是标题，旁白才是内容。** 画面文字必须比旁白**精炼得多**，一眼看明白，
不能把旁白原句搬上屏——观众要一边听一边读同样的话，两头都累。

上限（超了就是没做到位）：

| 元素 | 上限 |
|---|---|
| 主标题 | ≤ 10 字 |
| 副标题 / 说明行 | ≤ 14 字/行，最多 2 行 |
| 卡面说明（note） | ≤ 12 字/行，最多 2 行 |
| 反转卡每块（beat） | ≤ 12 字/行，最多 2 行 |

**删掉这些**：连接词（因为／所以／而且／不管／很多时候／有意思的是）、
铺垫从句、旁白已经讲清的因果、重复的主语。**只留一眼能抓住的那个点。**

示例（2026-08-27 实测的改法）：

- 「起英文名很多时候不查词义／只看这词听着吉不吉利、洋不洋气」（27 字）
  → **「不查词义／只挑吉利」（8 字）**
- 「所以你会遇到叫银行的、叫第一的、叫走运的／他们不觉得奇怪，那是父母的祝福」（35 字）
  → **「那是父母的祝福」（7 字）**

上线前必查：

```bash
python3 scripts/check_screen_text.py <project_id>
```

它扫 `cards`、`reveal`，**以及 `hook.poster` 的 t1/t2/plaque/foot**。
最后这一段是后补的：早先只往 `LIMITS` 里加了这几个键却没接上扫描，
于是「全部通过」对封面文案是空话 —— 点题行超宽一直没被拦下来。

## 🖼 封面第一帧必须同时适配横屏与竖屏 ★强制（2026-08-27 用户指令）

平台拿**第一帧**当封面，并按自己的版位裁切。1080×1920 的成片会被裁成：

| 版位 | 保留范围 |
|---|---|
| 9:16 竖屏 | y 0–1920（整帧） |
| 3:4 | y 240–1680 |
| 1:1 方形 | y 420–1500 |
| **16:9 横屏** | **y 656–1264** ← 只剩 608 像素 |

**封面核心带 = y 660–1260。主标题与关键视觉必须整个落在这里。**

### 封面版式：中文大标题海报 ★强制（2026-08-27 用户给了参考图）

**中文是主角，要精炼、放大、高亮。** 外文词降到带外的词表行。

核心带 660–1260 里三样，由 `PosterTitle` 自动排（按字数定字号、带内居中）：

| 元素 | 内容 | 字号 |
|---|---|---|
| t1 | 铺垫行，≤8 字 | 自动，≤132 |
| t2 | **点题行，最大最亮**，≤6 字 | 自动，≤196 |
| plaque | 描金匾里的副标题，≤12 字 | 自动，≤60 |

带外：`row`（外文词表）、`foot`（一行脚注）。**t1 恒小于 t2 的 0.75**，否则层级翻了。

组件在 `projects/_shared/Poster.tsx`，12 集各一份拷贝（引擎知识，不是创意组件）。
文案写在各集 `build_props.py` 的 `POSTER`，`check_screen_text.py` 校 t1/t2/plaque/foot。

#### 烫金字：三层，顺序不能动

`GoldText` 从下往上：**外发光（暖，模糊）→ 暗色描边层 → 字面（金属渐变）+ drop-shadow**。

**描边必须画在字面下面。** `-webkit-text-stroke` 是**居中**描边，往字身里吃进去一半；
只有让不带描边的字面盖在它上面，字身内部才干净，露在外面的只有描边的外半圈。

**试过 `paint-order: stroke fill` 把描边和字面画在同一个元素上 —— 这个 Chrome 不认，**
描边直接盖在字面之上，横画被吃掉一半，比原来更脏。别再试了。

反复踩的坑：

- **外发光必须在描边之外**，否则被描边整个盖住，字成哑光。
- **渐变的暗档要窄且不能深**。`#A9691A` 那种深棕压在字面中央 → 整个字像旧铜。
- **金色最亮那档不能接近纯白**（`#FFFDF3` 就凉了）。往琥珀压到 `#FFF2CE`／`#FFD97E`。
- **描边别用近黑、位移投影别做硬边** → 暖褐 `#5A2410`、厚度交给 drop-shadow。
- **描边宽度按缩略图定，不按满幅定 ★**（2026-08-27 用户：「中间部分有黑色的杂色」）。
  满幅 0.072×字号看着没问题，缩到审片页那种 330px 宽的缩略图，暗边就跟渐变暗档糊成
  一片黑。收到 **0.045×字号**、暗档提亮到 `#EFA829` 之后，小尺寸下整个字读作实心金。
  **改完一定要按缩略图尺寸再看一遍** —— 用户是在总览页上看到问题的，不是在满幅上。

#### 标题必须带「泰国 / 越南」的限定 ★强制（2026-08-27 用户指令）

t1＋t2 里**必须出现国名**。少了它，封面上的说法就成了普世断言
（「你我都得／看年纪换词」——哪国？），而且两个号的期号胶囊会长得一模一样
（都是「怎么称呼 · No.0X」），观众分不出这是泰语号还是越南语号。
期号胶囊也要带国名：`泰国称呼 · No.05` / `越南称呼 · No.04`。

#### 背景全程序化（没有插画素材可用）

图片生成全线不通，只有 Wikimedia 实拍。所以底纹全靠代码画：
十二瓣团花（或 `Glyph`：把这一集的目标字放大当底纹）、四角回纹、飘落花瓣、
底部山水剪影。**这一集有实拍就用 `PhotoFoot` 把图压进画幅下端**（泰01 的猪、
泰04 的西瓜），没有就用 `Landscape`。别让下半幅空着。

**不放光源 ★强制（2026-08-27 用户指令：「很强的光源，这个需要去掉」）**

早先 `PosterBg` 用锥形渐变做光束、再在光源处补一团亮核，结果每张封面顶上都挂一个
刺眼的光斑，喧宾夺主。现在只留**均匀的中心渐变 + 四角压暗** —— 有纵深，没有那个"太阳"。
`variant` 参数保留只为兼容调用，现在只影响底色明暗与重心。

#### 换某一集的装置时，别用「从装置注释切到 CardScene」这种切片

`s[s.index("/** 装置"):s.index("const CardScene")]` 会把**夹在中间的一切**一起换掉。
高频词各集的 `Chunk`（词组卡）正好长在那两者之间，这个切法已经连着两次
（hf-04、hf-03）把它删掉，渲染直接报 `ReferenceError: Chunk is not defined`。
换装置就只替换装置那一个组件，或者替换后回头确认 `Chunk` 还在。

#### 封面只放一句话 ★强制（2026-08-28 用户指令「封面过于复杂了」）

**核心带里只有一句话，两行合计不超过 12 个字。** t1／t2 不是「铺垫行 + 点题行」，
是**同一句话拆成的上下两行**，所以**两行等大**（用户原话：「我给你的字都需要大写，
而不是只大写其中的几个字」）。匾、词表、脚注一律不上封面 —— 那是把封面写成目录。

改这个的时候连着踩了两个坑，都属于「改了源头没落地」：

- **改完 `_shared/Poster.tsx` 一定要同步到 17 个项目**（各集有独立拷贝）。
  只改共享文件、不 `cp`，几种版式就还在跑旧代码 —— 这次 `left`/`stack`/`band`
  三种版式的第一行一直是小号眉题，就是漏了这一步。
- **改完 `cover_text.json` 一定要重跑 `build_props.py`**。渲染读的是 `props.json`，
  只写覆盖文件不重建 props，渲出来还是旧标题。

#### 平台版位的三组硬数字（2026-08-28 查证）

封面同时要过三关，**最紧的是 16:9**，但**最常被看到的是 3:4**：

| 版位 | 保留范围 | 什么时候出现 |
|---|---|---|
| 9:16 详情页 | y 0–1920 整帧 | 播放页 |
| **3:4 信息流** | **y 240–1680** | 推荐流、主页九宫格（最常见） |
| 16:9 横屏 | y 656–1264 | 横屏版位（最紧） |

抖音详情页还会盖住：**顶部 220px**（头像/昵称/关注）、**底部 380px**（互动数据）、
**左下角**（时长标签）。所以品牌行放 y≈296（躲开顶部遮挡、又在 3:4 里看得见），
带外脚注收在 y≤1420（躲开底部遮挡）。核心带 660–1260 三关全过。

#### 六种构图，每集分一种 ★强制（2026-08-28 用户指令：「封面需要调整，不能都一样」）

统一成海报版式之后，18 张封面变成同一张脸（品牌行＋期号＋两行标题＋匾＋词表），
只有配色在换 —— 一个号刷下来全一样。`PosterTitle` 现在有 **six 种构图**，
每集分一种，再配不同的纹样与底纹：

| layout | 长什么样 | 适合 |
|---|---|---|
| `center` | 居中双行 ＋ 描金匾 | 基础款 |
| `stack` | 外文词巨大压顶，中文点题在下 | 「一个词」类选题 |
| `left` | 左对齐贴边距，右侧一条竖金线 | 长标题 |
| `split` | 一条横金线劈成上下两半，错位排 | 前因后果型 |
| `frame` | 双描金框整个框住标题，像匾额 | 郑重的结论 |
| `band` | 通栏实色块压住标题（字幕条式） | 任何背景都要读得出时 |

六种都守同一条硬规矩：t1／t2／匾整个落在核心带 660–1260，字号按字数自动算，
t1 恒小于 t2 的 0.75。

两个已经踩过的实现坑（别再犯）：

- **`GoldText` 内部是 `position:absolute` + `width:100%`，必须放进一个有宽度的定位容器。**
  塞进 `display:inline-block` 宽度会塌成 0，字面直接废掉。左对齐要用 `align="left"`，
  不能靠外层容器去挤。
- **短视频封面的标题一律横排，不要做中文竖排。** 试过一版竖排（一字一行），
  它把标题挤成右侧一条窄柱、画面七成是空的，缩到 3:4 信息流缩略图只剩一条竖条 ——
  用户原话「太过诡异」。竖排是印刷海报的语言，不是短视频封面的语言。
  已换成 `band`（通栏色块）。

#### 泰文/越南文大字的行高

泰文上伸笔画（ไ）和声调符高出 em 框、下沉符（ู）低出；越南文叠标同理。
lineHeight 收到 1.05–1.2 必被裁或压行，大字一律 **1.25–1.5**。

## 📚 泰语高频词系列：一个词 × 五种角色（2026-08-27 重构）

老产线（`lang1000/marketing/video/series_high_freq_word/`，moviepy+PIL，已发 20 支）
的毛病：**一集塞 12 条用法、49 秒**，每条 4 秒；一屏 4 行密排表格；
谐音写成「瀑帕僑泰本」这种既读不出也不可靠；hashtag 烧进画面。

重构后：**一集 = 一个词 × 五种真正不同的语法角色**，**53–58 秒**。**不做谐音**，靠听两遍 + 跟读。

### 教的是词组，不是整句 ★强制（2026-08-28 用户指令）

第一版每张卡摆整句（`ฉันจะซื่อสัตย์กับคุณเสมอ` 24 个字符），
**对初学者太难**：念一遍两秒多，跟不上也记不住，片长还被顶到 80 秒。

现在每张卡的主角是**含目标词的 2–3 词组块**（`ที่ไหน` / `ก็ได้` / `ยิ้มให้`），
只念词组、只跟读词组；**整句降成底下一行灰色小字**当出处，不要求跟读。
语法说明行（note）一并去掉 —— 词组 ＋ 中文意思已经把话说完，再加两行解释是给初学者加负担。

**词组必须是原句的真子串**，`hf_chunk.py` 里有断言把关：
`assert chunk in card["th"]` 且 `assert card["thHi"] in chunk`。
这样词组仍然出自词库，不会变成我自己编的泰语。

**片长上限 60 秒。** 旁白也跟着收：每张卡只报「角色：词组中文义」，不再念整句。

### 选词与选句：只认词库，不凭印象

词表和例句一律取自 `lang1000/backups/sync_words/thai1000_*_all.json` 里
**已校对的 `example_sentence`**，一个字不编。选词时两条硬规矩：

1. **必须是 app 词条。** `นะ` 语料干净、钩子也好，但它不在词库里 ——
   教了 app 里没有，说不过去，弃掉。
2. **数句子时要排子串污染。** 泰语不分词，`ก็ in sentence` 会把
   `ภูเก็ต`（普吉）、`แจ็คเก็ต`（夹克）、`เก็บ` 全算进去 —— 看着 13 句，真实只有 6 句。
   用**词库自身的 1046 个词条**排：这次出现若被某个更长的词条整个盖住，就判为子串。
   （别用「前后不是泰文字母」当词边界 —— 泰语本来就不分词，那样会全否掉。）

已做：ที่ / ได้ / ให้ / เลย / กับ。备选（都验过语料）：`ก่อน` 11 句、`อยาก` 17 句、
`ช่วย` 23 句、`แล้ว` 48 句、`มาก` 163 句。

### 04 是**老版式的变体**，不走上面那套（2026-08-30 用户指令）

用户原话：「最早的高频词版本是非常成功的，看看从哪个版本进行调整做一个变体」。
最早那一代是 **2026-05-12** 的 `lang1000/.../make_thai_*_video.py`（一词一脚本，已发 20 支）。
把它翻出来看，它成在四件事 —— **我重构时把这四样全砍了**：

1. **一条给 12 个真用法**（3 页 × 4 行）—— 信息密度，观众觉得赚到
2. **表格式版面**，静音刷也能扫读
3. **逐行推进**，跟得住
4. **汉字 icon 当锚点**（门/灯/凉/点），一眼知道这一行讲什么

我那版（一集 5 张大留白词组卡）更"教学"，但把 1、2、4 全丢了。所以 04 不延续，
而是**从老版式做变体**：保留上面四条，去掉老版两个毛病 ——
烧屏 hashtag、12 条平铺流水账。

**谐音保留 ★（2026-08-30 用户指令「最后一列需要加中文谐音，不要之前的音标」）。**
我先前把谐音整个否掉是判断错了 —— 观众不看 IPA，`khâo-jai láew` 对零基础没用，
「考宅劳」才敢开口。老版「毕巴度／瀑帕僑泰本」的毛病是**乱拆音节**、又长又读不准，
不是谐音本身的错。现在的写法：**按泰语音节一一对应，一个音节一个汉字，不超 3 字**，
存在 `script.json` 的 `rows[].xie`；词头也一并换成「谐音『劳』」——
**一张表上不要同时出现音标和谐音**，两套注音混排比没有更乱。

**变体点（这一集自己的视觉语言）**：แล้ว 是完成态，所以**讲过的行不还原，
打上勾、沉下去留在表里**。老版是高亮扫过就复原；这一版扫过就"结掉"，
到每组末尾四行全部打勾、再一起亮起连读一遍 —— 画面自己在演这个词。

**12 条按 แล้ว 的三种用法分三组**（事做完了 / 情况变了 / 然后·了吗·接话），
这是把我那版「语法角色」的好处，嫁接回老版「高密度表格」的骨架上：
观众记住的不是 12 个短语，是这个词的三种用法。**每组末尾连读一遍 + 跟读空档**，
把系列的「读两遍 + 跟读」纪律接了回来，同时片长仍压在 **59.8 秒**。

12 条全部有 thai1000 语料出处（例句真子串或 app 词条）。配色 **深松绿 + 琥珀金**，
跟 01 炭墨+橙 / 02 墨蓝+青柠 / 03 暖褐+砖红 / 05 暗蓝+天蓝 岔开。

**泰语按词分开显示 ★（2026-08-30 用户指令「泰语的部分需要把词分开」）。**
泰语本来不分词，`เข้าใจแล้ว` 连成一片，学习者看不出哪一段是目标词。
现在拆成 `เข้าใจ | แล้ว` 存在 `rows[].thParts`，**只影响画面**——
**配音仍用连写的 `th`，泰语 TTS 遇空格会断读**。
拆法与谐音一一对应（`ไป แล้ว`＝拜劳 / `แล้ว ไป`＝劳拜），目标词满色、其余压到 0.72，
于是词序换了一眼就看得出来。生成时有断言把关：`"".join(thParts) == th`，
分词必须能还原成原词，不会拆出一个我自己编的泰语。

两个当场踩到的：

- **分组标题的配音漏挂了**（只挂了行音频和连读音频）。是 `verify_audio_coverage.py`
  的相对判据抓出来的：那三段量到 −45.6/−43.5/−42.3dB，比垫乐高不出 6dB。
  固定阈值 −45dB 只能抓到其中一段 —— **相对判据这次又救了一回**。
- **表格别只占上半幅。** 第一版 ROW_H=158 / TABLE_TOP=700，四行落在 700–1330，
  底下空 590px，"信息密度"的感觉就没了。收到 ROW_H=196 / TABLE_TOP=748 才撑住。

### 每集的装置必须不同，且从这个词自己的逻辑长出来

| 集 | 词 | 装置 | 配色 |
|---|---|---|---|
| 01 | ที่ | 竖直标尺：高亮块钉在同一位置，五种角色 | 炭墨 + 橙 |
| 02 | ได้ | 横向等式：`中文词 ＝ ได้`，右边永远不变 | 墨蓝 + 青柠 |
| 03 | ให้ | 箭头：这个词管方向，词嵌在箭身上 | 暖褐 + 砖红 |
| 04 | เลย | 语气刻度：不改意思只改语气，指针停在不同档 | 深墨绿 + 亮黄 |
| 05 | กับ | 连接扣：两块方牌 + 中间一枚扣子 | 暗蓝 + 天蓝 |

共同纪律：**全片只有一个强色**，只给目标词和它的中文对应词；
中文句与泰语句里的对应部分**同色高亮**（这是教学核心：中文换了五个词，泰语是同一个）；
收束卡把五个角色和五个同样的目标词并排。

## 🔁 高频词重做批：16 个已发布的词，同一版式换配色配图（2026-08-30 用户指令）

用户原话：「用这种方式把最开始已经发布的高频词重新做一遍，要求每一个都需要调整背景颜色，
可以增加必要的配图」。「这种方式」＝ hf-04「แล้ว」定下的老版式变体。

**16 个词**（`projects/thai-1000-hfx-01…16-*`）：ao / chai / dai / dii / duu / hai / khaw /
khong / kin / maa / mii / pai / ruu / tham / yaak / yuu。名单不是猜的 ——
`lang1000/.../series_high_freq_word/泰语千词_已发布/` 里的 `thai_<slug>_cover.jpg` 就是发布清单。

**内容逐条沿用老脚本，一个字不重编。** `gen_hf_remake.py` 直接 `ast.literal_eval`
老脚本里的 `PAGES`（12 条 icon/中文/泰语/罗马音/谐音），所以 12 条用法和谐音都是当年校过的。

相对老版改的五处：

1. 罗马音 → **中文谐音**（老版第 5 个字段本来就有，只是没上屏）
2. **泰语按词分开**显示，目标词满色、其余压 0.72
3. 12 条**按语义分三组**，每组末尾连读一遍 + 跟读空档
4. 去掉烧屏 hashtag
5. **每集换底色 + 一张 Wikimedia 实拍配图**（压在表格下方当纹理，opacity 0.30 再压一道本集底色）

### 越南语同批（15 个，series_id=2）

已发布名单来自 `lang1000/.../vietnamese/series_high_freq/发布文案.md` 的 `## No.NN` 标题：
Ở / Có / Đi / Là / Muốn / Cho / Được / Làm / Ăn / Đến / Biết / Lấy / Xin / Xem / Tốt。
内容从 `viet_highfreq.py` 的 `HFEpisode.pages` 解析，同样一个字不重编。

两处与泰语批不同：

- **越南语本来就用空格分词**，显示分词直接按空格切，不需要泰语那种复合词覆盖表。
- **老越南语脚本没有汉字 icon**（泰语那批有）。从中文释义推：去掉目标词的意思，
  取第一个**还没被本集占用**的字 —— 同一集 12 个必须互不相同，重复了就不是锚点。
  启发式跑完只有 `ep02_co` 撞一次（「有人」「有人吗」都想要「人」），手工定成「谁」「了」，
  其余 14 集自动通过，`assert len(set(icons)) == 12` 兜底。

**edge-tts 批量会被限流 ★（这一批实测）**：15 集 × 34 段连着跑，中途 5 集整集
`NoAudioReceived` 三次重试全败；单独跑同一集立刻就成。**并发重试只会互相抢**，
正确做法是**串行 + 集间 sleep**，失败的挑出来单独补。别加重试次数，那样只是把限流拉长。

`push_to_videopublisher.py` 加了 `SUBDIR_BY_SERIES`：越南语高频词的老目录叫
`series_high_freq`（没有 `_word` 后缀），跟着老目录走，别新开一个平行目录。

### 这个系列**刻意不换版式** ★

CLAUDE.md 上面那条「同一系列每集必须换视觉语言」针对的是**禅意那种一集一个概念**的系列。
**高频词是格式化栏目**：老版 20 支同一张脸，观众一眼认出这是哪个栏目 —— 这正是它成功的原因之一。
所以这一批的变化项由用户明确指定为**配色与配图**，版式 16 支完全一致。
别拿那条通则来"纠正"这里。

### 三个实现要点

- **分词有覆盖表。** 机械地在目标词处切开会劈坏复合词：`ดูแลตัว` 里 `ดูแล` 是「照顾」，
  切成 `ดู|แลตัว` 就错了。`SPLIT_OVERRIDE` 手工兜住 4 个：`ดูแลตัว` / `ขอโทษ` /
  `ของขวัญ` / `ของฝาก`（后三个是固定说法，拆开反而误导）。断言 `"".join(parts) == chunk` 兜底。
- **配图只走 Wikimedia**（本机唯一能用的图源）。下载后压到 1080 宽、`saturation=0.42`、
  轻模糊 —— 它是底纹不是主角。署名存 `artifacts/photo_credit.json`。
- **配色从 props 注入**，`HF.tsx` 是 16 支共用的一份（`projects/_shared/HF.tsx`）。
  `Backdrop` 的颜色**显式传参**，不靠模块级变量的赋值时机。

片长 53.7–59.4 秒，全部在 60 秒内；34/34 有声，画面文字自查 16/16 通过。

## 🧘 禅意系列：文案保留，画面配乐重做（2026-08-29）

老的 `thai-1000-zen-01-impermanence` 有四个毛病：**540×960**（平台要 1080×1920）、
**一个项目塞了 6 支不同的片子** + 47 个中间版本共 53 个 mp4、
**没有任何 artifacts**（没有 script/props/检查点，校验器和事实面板全失效）、
走的是已经不用的 hyperframes 产线。

重做的做法：**文案逐字保留**（钩子问句、整句、逐字拆解、片尾），
画面与配乐重做，输出 1080×1920，每支单独立项（`thai-zen-<nn>-<slug>-v2`）。

两条这一集踩出来的：

- **泰语整句不能交给浏览器断行。** Chrome 按词典断泰语，`ตลอดไป` 会被拆成
  `ตลอด|ไป`；加空格分词也救不了。**把整句写死成几行**，断点自己定。
- **封面即第一帧，钩子不能做淡入。** 给钩子加 34 帧淡入，第 0 帧就是一整屏空的，
  平台抓去当封面是一片黑。起手即成品，只在末尾淡出。

### 封面整张由 ChatGPT 出，代码不再画任何元素 ★（2026-08-29 用户指令）

本机 12 个出图工具全不通，封面**整张交给 ChatGPT**：背景、圆相、品牌行、期号、金线、
中文标题、泰文、朱红闲章全在图里，`Hook` 组件只剩旁白，`CoverPlate` 只做一件事 ——
铺图、末尾 26 帧淡出交回正片空镜。**不要再往生成图上叠罩子**：图里已经有文字和明暗，
再压一层只会把它压灰（这一版把之前那两层渐变全删了）。

提示词页：`projects/_shared/cover_prompts.html`（各段独立、带复制按钮、
**文件名写在提示词最后一行**、每张附逐字校对清单和一段「修图提示词」）。
图放进 `projects/_shared/cover_src/`，`scripts/import_cover_src.py` 拉到 1080×1920
并量标题带亮度（画面 39%–55%，YAVG 上限 90）。

**出图模型渲外文文字的实测结果（值得记住）**：中文标题、期号、闲章字**五张全对**；
泰文 **4/5 全对**，声调符位置都准 —— 比预期好得多。唯一一处错在 05：
`ก็` 出成了 `กั`（ก 上面该是尖顶的 ็，出成了圆圈的 ั）。所以**必须逐字符核**，
放到 8× 看那些区别只在一个记号上的字。

**已知没修的两处**（用户 2026-08-29「直接做吧 差一点没有关系」）：

1. 05 的泰文 `กั` 应为 `ก็`。
2. 五张的品牌行在 **3:4 信息流版位被拦腰切断** —— 模型把它放在画面高度 11.7%，
   而 3:4 裁掉顶部 12.5%。要修就重出一轮，**别在代码里补画一行**（那样图上会有两行）。

**顶部两行的百分比，模型怎么都不听 ★（2026-08-29→30 两批共十张实测）**

第一批写 15.5%，模型放 11.7%（被切一半）；第二批写 17.5% 并加了
「15% 以上不许有任何文字」，越南语那批模型改成放 **5%–12%**（3:4 里干脆整行不见）。
**往上顶是这类版式的默认习惯，靠加约束句改不动。**

改法不是继续调百分比，是**换设计**：把品牌行和期号从画面顶端挪到
**金线和标题正上方的空白里**（31% / 34%）。它们和标题成为同一组，模型就不会再往上跑，
而且 31% 本来就在三种版位的安全区内。两个提示词页都已经照这个改了。

### 封面要有禅意：先做明暗，再谈笔墨 ★（2026-08-29 用户两轮否：「有点平」→「还是不行」）

第一轮我以为"平"是构图对称，改成不对称 + 巨字 + 圆相 + 闲章 —— 还是不行。
**满幅看才明白：真正的"平"是明暗被拍掉了。** 罩子 0.46 + 底片 contrast 1.05，
整幅压成一片中灰，没有亮区也没有暗区。这种情况下加多少装饰都白搭。

四条按重要性排（顺序不能倒过来）：

1. **明暗第一。** 底片分级从 `contrast=1.05, brightness=-0.02` 提到
   **`contrast≈1.20, brightness≈+0.03`**，罩子从 0.46 **减到 0.30**，暗改压在四角与下端
   （`radial-gradient` 收到 70%/50%，外圈加深到 0.62）。天空亮、山黑、字在中间 —— 层次出来了。
2. **字要细。** 标题从 `SerifSC900` 换成 **`SerifSC300` + `letterSpacing: 6`**。
   900 的重衬线压在实拍上就是鸡汤海报；禅意要细、要疏。
3. **笔墨不是几何。**
   - 圆相原来是"等粗圆 + 一个缺口" = 加载动画的圈。现在用**填充路径**画：
     外缘半径恒定、内缘沿角度渐变，笔画从起笔粗一路收到收笔细，末端出锋，再叠一点手抖。
   - 巨大的淡汉字是一整块均匀的灰，糊在下半幅像脏。换成 **`InkRidge` 远山剪影**
     （三层，越远越淡越高）：意境靠远近，不靠水印。
   - 闲章原来是圆角矩形 + 投影 = app 图标。现在是**不规则刻边 + 斑驳 + 微微歪**，
     并收到 74px —— 它是画面的定海针，不是按钮。
4. **圆相当"月"放天上，别和闲章挤在同一角。** 两个都在右下时读成一团。
   圆相抬到 y≈470（天空区），闲章贴在泰文那一行行尾。

**加字重必须连 loader 一起重生成。** `fetch_cjk_fonts.py` 以前只写 `cjk_faces.json`，
`CjkFonts.tsx` 是手工维护的 —— 于是 300 字重下载好了、组件也引了 `ZH_ZEN`，
渲出来还是 900。现在脚本把 loader 一起生成（`write_loader`），
并且 `Promise.all` 换成 **`allSettled`**：原来一个分块加载失败会整批 reject，
结果是**一个字体都没装上**，全片回落系统字体。

### 封面要有禅意（第一轮的结论，仍然有效）

原来的禅意封面是：一条横线 → 两行居中大字 → 一条横线 → 泰文。
**左右对称 + 所有元素同一层 = 平。** 加底片、换配色都救不了，因为问题在构图。

三件东西一起上才立得起来（组件在 `projects/_shared/ZenCover.tsx`，各集一份拷贝）：

1. **破对称。** 横线**只留一条**且不居中；整块标题压向一侧（01/03/04 靠左、02/05 靠右），
   另一侧空着给圆相。对称的那一对横线是把画面钉平的元凶。
2. **纵深三层。** 远：一个**巨大的淡汉字**（这一集的字眼：常/放/静/足/息）出血到画外，
   `op≈0.045`；中：标题与泰文；近：闲章。**巨字必须出血**——完整摆进画里就又成装饰了。
3. **一枚朱红闲章。** 通篇冷灰里唯一的暖点，约 80–84px。中式立轴就靠这一枚章定住画面，
   它同时也是缩略图上唯一的彩色识别点。

再加一个**圆相**（enso，禅宗的破圆）：`Enso` 用 `strokeDasharray` 留缺口 +
末段更细的第二条弧做收笔提按。**不能闭合**——闭合了就是个圈，不是圆相。

摆位每集不同（这才是创意部分）：01 圆相在左下对着空处 · 02 在右上"放开手"的位置 ·
03 压在水线上一半沉进水里（月在水上也在水里）· 04 是"多"里的那个"一" ·
05 跟着呼吸张合，把正片的装置提前露一手。

三个当场踩到的：

- **改完标题字号要数一遍字宽。** 90px × 9 字 = 810px 塞不进 800px 的框，
  01 的「的?」被挤到第三行。收到 84px、框放宽到 890 才对。
- **巨字别放在顶部。** 第一版 03 的「静」压在品牌行和期号上，糊成一片。挪到左下角出血。
- **闲章和圆相会撞。** 05 两个都在左下，重叠了；元素多了必须逐个核位置，不能只看单张。
- **元素叠够四样就要减。** 04 有点阵 + 巨字 + 圆相 + 闲章，太挤 —— 巨字撤到右下角只留一角。

### 五集各自的装置（2026-08-29 全系列重做完）

| 集 | 句子 | 装置 —— 从这一句自己的逻辑长出来 | 配色 |
|---|---|---|---|
| 01 无常 | 没有什么能永远陪着我们 | 词讲完就退掉不再回来；底部时间线上讲过的点一个个熄灭 | 冷墨青 + 暖金 |
| 02 放下 | 放下，并不是忘记一切 | 词讲完**沉到下端排成一列留着**（01 的反面：放下≠清空） | 暖夜 + 琥珀 |
| 03 平静 | 心的平静，不是没有问题 | 一条水线，词立在上、倒影在下轻晃；念「问题」时倒影起涟漪又归于平 | 青蓝 + 月白 |
| 04 知足 | 幸福，不是拥有得多 | 满屏点阵代表「多」，每讲一个词熄一批，最后只剩文字下方那一点 | 暗紫 + 蜜色 |
| 05 重新开始 | 只要还在呼吸，就能重新开始 | **整幅画面按 4 秒一次呼吸**（吸1.6/停0.4/呼2.0），词在吸气那档亮起 | 墨绿 + 晨光金 |

老片 02–05 的生成脚本已经不在了，**文案是从老 mp4 逐帧读回来的**（`ffmpeg fps=1/3.6 tile=6x2`
拼成联络表再读），一个字没改。选题名对应：02 letgo / 03 calm / 04 enough / 05 beginagain。

两条这一轮踩出来的：

- **垫乐必须先校准电平再用。** 本地那 13 条 Pixabay 曲子原始电平差了 17dB
  （`m_bowl` −27.4 / `zen_bed` −10.1）。不校准的话 props 里那个 `gain` 在不同集里
  含义完全不同 —— 02 的垫乐量到 **−49.5dB**（等于没放），05 量到 −30.9dB（压住人声）。
  `scaffold_zen_v2.py` 现在一律先归一到 **−13.8dB**（01 的 `warm_piano` 就是这个电平），
  之后 `gain≈0.085` 在每一集都落在 −35dB 左右。
- **装置里的「唯一那一个」别放在正中。** 04 原来把幸存的那一点放在点阵几何中心，
  正好压在泰文大字上，读成一个杂点。挪到文字带下方（y≈1318）之后，
  「一堆 → 一个」才看得出来。

### Sequence 的 from 是相对本场景的 ★（2026-08-29 用户听出来的）

`props` 里的 `audioAt` / `thAt` 是**全片绝对秒**，`<Sequence from>` 是**相对外层 Sequence** 的。
词卡一直写的是 `(w.zhAt - w.start)`，整句／再读／片尾三处漏了减 `start` ——
`from` 被推到场景长度之外，**这四段旁白根本没播**。用户听出片尾没说
「泰语千词，在泰国生活够用了」，我才发现。

**校验器也没拦住，因为判据是错的。** 原来用固定阈值「<−45dB 算静默」，
但那几段底下还垫着 BGM，量到 −44.7dB —— 比阈值高，于是报「静默 0 段」。
**垫乐本身就有能量，固定阈值分不清"没人说话"和"有垫乐"。**
`verify_audio_coverage.py` 已改成**相对判据**：先量纯垫乐中位数，
每段旁白高不出 **6dB** 就判「没响」，并逐段列出时间；有没响的段就返回退出码 2。

### 背景空镜：本地 Coverr 素材，不走 API（2026-08-29 用户「参考之前的视频 可以增加背景视频」）

老禅意系列（`thai-1000-zen-01-impermanence`）的实拍底片来自
`lang1000/marketing/video/series_zen/assets/bg/` —— **五条 Coverr 空镜，早就下在本地**
（免费授权、无需署名）。重做版第一版做成了纯程序化墨底，画面偏空，这批素材是现成的。

**别再去调 `pexels_video` / `pixabay_video`。** 老项目的 `events.jsonl` 里
33 条 stock 视频调用**没有一条成功**（key 是空占位符，见记忆
"OpenMontage stock keys broken"）；那条实拍底片是从这个本地目录来的，不是抓来的。

做法（`prep_bg.py`，三件事都在 ffmpeg 里做完，不在 Remotion 里做）：

1. **1920×1080 横屏中心裁到 9:16 只剩 608px 宽**，放到 1080×1920 是 1.78× 上采样。
   所以必须去色降对比 + 轻模糊（`saturation=0.58, gblur=sigma=1.2`），
   让它读作"底纹"而不是"糊掉的实拍"。
2. **素材不够长就 `setpts` 放慢，不要循环** —— 空镜放慢看不出来，接缝一眼能看出来。
   本集 bg5 放慢 1.61×（10.97s → 17.6s 的日落云）。
3. 每段多渲 1 秒尾巴，交叉溶解在 Remotion 里做。

两条实现纪律：

- **底片挂在顶层，只挂一次。** 放进各场景里的话，每个场景都会把视频从头播一遍。
  原来的 `Backdrop` 因此拆成 `BgFilm`（连续底片）+ `Atmosphere`（半透明墨罩），
  两层都在 `Impermanence` 顶层，场景里只剩文字——场景淡入淡出也就只影响文字。
- **第一段空镜不做淡入**（`first={i === 0}`）。同「钩子不做淡入」一个道理：
  第 0 帧就是封面，淡入等于把封面渲成黑屏。

**分段边界从内容来，不是按秒切**：钩子+整句一条、词卡两两一条、末词+再读+片尾一条。
空镜"换了就不回来"，跟这一集词卡"讲完就退掉"是同一个意思——底片也在说无常。

**字压得住底片靠那条竖向渐变**，不是靠把整体压黑。整体压到 0.60 时画面等于白加，
现在墨罩 0.46，正文带（44%–58%）单独再压一道，两头亮、中间沉。

### 片长：收死时间，不收留白（2026-08-29 用户「时间上可以再压缩一下」）

禅意靠留白说话，但**留白 ≠ 空转**。第一版 68.8 秒里，旁白只占 27.7 秒，
剩下四十秒全是固定停顿——起手 1.0s、句间 0.8/0.6s、词卡两遍之间 0.9s、
跟读 1.5s、片尾 1.8s，一律不看内容长短。

收法（**59.1 秒**，进 60 秒内）：

- 固定留白各砍三到四成（起手 1.0→0.6、句间 0.8→0.55、两遍之间 0.9→0.55、片尾 1.8→1.4）。
- **跟读空档改成按词长走**：`max(0.95, td + 0.35)`。跟读一个词要的时间跟这个词一样长，
  固定 1.5s 对 `กับ`（0.55s）是空转、对 `ตลอดไป`（1.38s）反而紧——现在短词收紧、长词放宽。

**别用降 fade 时长来省秒数**：淡入淡出是这一集的视觉语言，砍了就成幻灯片。
要省就省停顿。

### 沉稳男声 = 音色 + 降速 + 压调，光换音色不够 ★（2026-08-29 用户指令）

edge-tts 的中文男声**一共只有四个**：Yunjian（Sports/Novel · Passion）、
Yunxi（Novel · Lively/Sunshine）、Yunxia（Cartoon · Cute）、Yunyang（News · Professional）。
按标签挑，`Yunyang` 看着最"专业"——但它是**新闻播报腔**，语调往上挑、节奏推着走，
用在禅意上就是"念稿"，用户听完的原话是「这个男声不行」。

现在用 **`zh-CN-YunjianNeural` ＋语速 −15% ＋音调 −4Hz**（2026-08-29 用户听完五个候选选的 A）：
Yunjian 是叙述/评书底子，胸腔共鸣厚，本身就够沉；**压调只需要轻轻一档**。
我自己先推的是 −20%/−12Hz（候选 B），用户选了压得更轻的 A ——
压过头会闷、会假，"沉稳"是音色带来的，不是把音调硬拉下去砸出来的。
`synth_narration.py` 为此加了 `metadata.tts.pitches`（Hz 偏移，`--pitch=` 等号写法）。

**换音色一定要试听再定。** edge-tts 零成本，五个候选一次跑完（`artifacts/voice/` + `voice.html`），
比渲完整片再被打回便宜得多。（成本纪律里的"不试音"针对付费引擎，edge-tts 不在此列。）

## 🧘 越南语禅意一句（2026-08-29 立项，series_id=2）

对标泰语那一支：一句话 + 逐词拆解 + 跟读，36–41 秒，五集。**这一支没有老片，
句子是新写的**，所以定了一条更硬的规矩：**卡上教的实词一律取自 viet1000 词库**，
`scripts/scaffold_zen_viet.py --check` 逐个核，不过就不许开工。

虚词（không / có / gì / là / phải / hết）这套词表本来就不收 —— 它是**短语型词表**
（收的是「Tôi không hiểu」这种整句），所以不能要求每个词都是词条，只卡实词。
另有一个坑：词表里有 **「Rất / Nhiều」这种一条收两个词**的写法，
按斜杠拆开都要算词条，否则 `Nhiều` 会被误判成"不在词库"。

| 集 | 句子 | 装置 | 配色 |
|---|---|---|---|
| 01 无常 | Không có gì là mãi mãi. | 固定方框，词被一个个顶进来又顶走 —— 位置永远在，内容永远换 | 炭青 + 灰白 |
| 02 放下 | Buông bỏ không phải là quên hết. | 一对括号钳住词，念第二遍时张开把词放出去 | 暖褐 + 陶红 |
| 03 平静 | Bình tĩnh không phải là không lo lắng. | 一条起伏的线：Lo lắng 波幅拉满，Bình tĩnh 收成直线 | 青灰 + 湖蓝 |
| 04 知足 | Hạnh phúc không phải là có nhiều. | 天平：每讲一词摆一次，最后停平 | 靛蓝 + 米金 |
| 05 重新开始 | Hôm nay khó, ngày mai bắt đầu lại được. | 地平线上的光一档档升高，最后整条亮起 | 墨蓝 → 晨金 |

配音：越南语 `vi-VN-HoaiMyNeural` −22%，中文旁白沿用泰语支的 `zh-CN-YunjianNeural` −15% / −4Hz。
底片仍是那五条 Coverr 空镜，靠**不同的裁切位（`bg_spec.json` 的 `x`）和分级**跟泰语支岔开。

**组件生成方式**：`scripts/gen_viet_zen_components.py` —— 底盘
（BgFilm / Atmosphere / CoverPlate / Sentence / Outro / 根组件）是一段共用模板，
**装置和词卡各集独立写在 `EPS` 里**。这符合 atelier 原则：复用引擎知识，绝不复用创意组件。

封面同样整张由 ChatGPT 出：`projects/_shared/cover_prompts_viet.html`。
**越南语的风险点是叠标**（ắ ầ ỏ ĩ ệ 一个字母上叠两个记号），模型容易漏一个，必须逐字母核。

## 🎬 交片方式：一律生成本地审片页 ★强制（2026-08-26 用户指令）

**每次渲出新版本后，必须重新生成本地审片页并交给用户，不要只丢一个 mp4 路径。**

```bash
python3 scripts/build_review_page.py <project_id> --open   # 这一条片子的审片页
python3 scripts/build_review_index.py --open               # 所有片子的总览列表页
```

`review_index.html` 在仓库根目录，列出全部项目：版本数、当前片长/大小、成本、
是否已登记发布、流水线进度，并可在页面上标 **待审 / 通过 / 打回** 加备注。
磁盘能推出来的状态自动读，人工结论存本页 localStorage，「复制审核结论」贴回对话。
**每次渲完或发完，两个页面都要重新生成。**

产物是 `projects/<project_id>/review.html`——纯本地页面，双击即开，不联网、不发布。
页面内容全部读自 `artifacts/` 与 `composition/props.json`，**不要手改 HTML**；
改完一版重跑一次即可同步。页面提供：

- **版本切换**，切换时停在同一时间点，方便逐版 A/B
- **时间轴**，点击跳转到该场景；列出起止时间、词条、该段旁白原文、配图 CC 署名
- **审核清单**，勾选状态存浏览器本地；条目是本产线真踩过的坑
- **问题记录**，「复制审核意见」把版本号＋清单通过率＋未确认项＋笔记打包，用户贴回对话
- **只读参照**：本版事实（片长/成本/运行时）、当前生效的决策、已做校验、已知问题

### 封面文案在审片页上手工调（2026-08-28 用户指令）

封面的关键词现在**不用改代码**。审片页有「封面文案 · 可直接改」一栏，
六个字段（期号 / 铺垫行 / 点题行 / 匾内副标题 / 词表 / 脚注）都是输入框，
右侧实时显示字宽并在超限时标红（全角算 1，半角算 0.5，上限与
`check_screen_text.py` 一致）。改完点「复制封面文案」拿到一段 JSON，贴回对话：

```bash
pbpaste | python3 scripts/apply_cover_text.py -     # 也支持贴多条（JSON 数组）
```

**每集还预置了 5 组备选核心词**（`<project>/artifacts/cover_alts.json`），
在输入框下方以标签形式排开，点一下就把「铺垫行 + 点题行」一起换掉，当前在用的那组高亮。
备选是**换说法，不是换事实** —— 不能说出这一集没讲过的东西。
要换备选就改那个 JSON，页面重建即可。

脚本把它写成 `<project>/artifacts/cover_text.json`，各集 `build_props.py`
读到就覆盖内置的 `POSTER` 与 `badge`。**所以改封面文案不会被下次重跑
build_props 冲掉**，也不用去动 17 个脚本。写完自动重建 props，然后照常重渲。

**改文案必须回写 `artifacts/script.json`。** 审片页的旁白读的就是它。
曾经出现过只改 TTS 生成脚本、不回写 script.json，导致审片页显示的是三个版本之前的旁白。
生成器现在会检查 `script.json` 是否比 `props.json` 旧，旧了就在页面顶部红条告警——
**看到告警先修数据，别改页面**。

交付话术：给出 review.html 路径并说明已打开，而不是让用户自己找 mp4。
成片仍然照常放在 `projects/<project_id>/renders/`。

新项目若 `props.json` 结构与卡片式不同，页面会退化成「播放器＋事实面板」；
需要时间轴就去 `collect_scenes()` 补该结构的解析，别在页面里写死内容。

**MANDATORY: Read [`AGENT_GUIDE.md`](AGENT_GUIDE.md) before responding to ANY user message.**

Do not act on the user's request until you have read AGENT_GUIDE.md.
It contains routing rules that determine your first action based on what the user asked.
Skipping it WILL cause you to take the wrong action.

There are no instructions in this file. All instructions are in AGENT_GUIDE.md.
